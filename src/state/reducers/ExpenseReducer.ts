const INITIAL_STATE = {
    expense: []
};

const ExpenseReducer = (state = INITIAL_STATE, action: any) => {

    return state;
    // switch (action.type) {
    //     case "FETCH_USER":
    //         return {
    //             ...state,
    //             isLoading: true,
    //             error: false,
    //         };
    
    //     default:
    //         return state;
    // }
};

export default ExpenseReducer;